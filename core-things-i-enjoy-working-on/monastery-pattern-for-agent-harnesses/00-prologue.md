# The Monastery Pattern for Agent Harnesses

A harness that keeps its discipline: a small, closed set of rules an agent works inside, held deliberately narrow so behaviour stays predictable and the work compounds instead of drifting.

## Open questions

- What is the minimum rule set that keeps an agent faithful to its original perspective?
- How does the harness detect drift rather than trusting the agent to report it?
